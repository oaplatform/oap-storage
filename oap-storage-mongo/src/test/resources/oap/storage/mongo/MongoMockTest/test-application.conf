{
  profiles = [
    mongo-mock
  ]

  services {
    mongo-client.parameters.host = localhost
    mongo-client.parameters.port = ${MONGO_PORT}
    mongo-server-mock.parameters.port = ${MONGO_PORT}
  }
}
